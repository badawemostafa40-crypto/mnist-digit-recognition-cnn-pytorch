# mnist-digit-recognition-cnn-pytorch
A university-level Deep Learning project using PyTorch and CNN for handwritten digit recognition on the MNIST dataset. The project includes data preprocessing, CNN implementation, training, evaluation, optimizer comparison (Adam vs SGD), confusion matrix, classification report, and performance visualization.
